package com.example.fieldworks.jpetstore_boot;

import org.junit.jupiter.api.Test;
import org.springframework.boot.test.context.SpringBootTest;
import org.springframework.context.annotation.ComponentScan;

import static org.junit.jupiter.api.Assertions.assertTrue;

//@SpringBootTest
//@ComponentScan(basePackages = "com.example.fieldworks.jpetstore_boot")
class JpetstoreBootApplicationTests {

	@Test
	void contextLoads() {
		// This test method is intentionally left empty to check if the Spring application context loads successfully.
		assertTrue(true);
	}
}
